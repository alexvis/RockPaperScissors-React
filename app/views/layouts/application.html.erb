<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Rock Paper Scissors" %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="top-bar">
      <li class="home"><%= link_to 'Home', root_path, class: "home"  %></li>
      <% if !user_signed_in?%>
        <li class="right"><%= link_to 'Sign Up', new_user_registration_path, class: "right" %></li>
        <li class="right"><%= link_to 'Sign in', user_session_path, class: "right" %></li>
      <% else %>
        <%= image_tag(current_user.profile_photo, class: 'picture') if current_user.profile_photo? %>

        <li class="right"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "right" %></li>
        <li class="right"><%= link_to 'Edit Account', edit_user_registration_path, class: "right" %></li>
      <% end %>
    </div>
    <div class="page">
      <%= yield %>
    </div>

      <div class="footer large-12 medium-12 small-12 columns">
        <a class="fa fa-github" href="https://github.com/alexvis"> Github</a>
        <a class="fa fa-linkedin" href="https://www.linkedin.com/in/avisconde"> Linkedin</a>
      </div>

    <%= javascript_include_tag "application" %>
  </body>
</html>
